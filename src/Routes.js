import { BrowserRouter as Router, Route, Switch } from "react-router-dom";

const Routes = () => {
  return (
    <Router>
      <Switch>
        {/* Routes placed here */}
      </Switch>
    </Router>
  )
}

export default Routes
